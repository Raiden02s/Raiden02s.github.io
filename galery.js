//-------------------------------------------------------------IMAGE2--------------------------------------------------------
function image2(){
    $( "#dialog1" ).dialog( "open" );
  }

  //auto open window

$( "#dialog1" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog1" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog1" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog1" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog1" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog1" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog1" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog1" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog1" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog1" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog1" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog1" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog1" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog1" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog1" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog1" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog1" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog1" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog1" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog1" ).dialog( "option", "show" );
 
// Setter
$( "#dialog1" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE3--------------------------------------------------------

function image3(){
    $( "#dialog3" ).dialog( "open" );
  }

  //auto open window

$( "#dialog3" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog3" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog3" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog3" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog3" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog3" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog3" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog3" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog3" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog3" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog3" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog3" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog3" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog3" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog3" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog3" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog3" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog3" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog3" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog3" ).dialog( "option", "show" );
 
// Setter
$( "#dialog3" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE4--------------------------------------------------------

function image4(){
    $( "#dialog4" ).dialog( "open" );
  }

  //auto open window

$( "#dialog4" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog4" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog4" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog4" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog4" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog4" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog4" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog4" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog4" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog4" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog4" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog4" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog4" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog4" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog4" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog4" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog4" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog4" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog4" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog4" ).dialog( "option", "show" );
 
// Setter
$( "#dialog4" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE5--------------------------------------------------------

function image5(){
    $( "#dialog5" ).dialog( "open" );
  }

  //auto open window

$( "#dialog5" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog5" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog5" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog5" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog5" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog5" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog5" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog5" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog5" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog5" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog5" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog5" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog5" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog5" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog5" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog5" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog5" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog5" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog5" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog5" ).dialog( "option", "show" );
 
// Setter
$( "#dialog5" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE6--------------------------------------------------------

function image6(){
    $( "#dialog6" ).dialog( "open" );
  }

  //auto open window

$( "#dialog6" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog6" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog6" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog6" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog6" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog6" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog6" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog6" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog6" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog6" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog6" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog6" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog6" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog6" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog6" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog6" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog6" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog6" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog6" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog6" ).dialog( "option", "show" );
 
// Setter
$( "#dialog6" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE7--------------------------------------------------------

function image7(){
    $( "#dialog7" ).dialog( "open" );
  }

  //auto open window

$( "#dialog7" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog7" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog7" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog7" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog7" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog7" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog7" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog7" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog7" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog7" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog7" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog7" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog7" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog7" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog7" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog7" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog7" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog7" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog7" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog7" ).dialog( "option", "show" );
 
// Setter
$( "#dialog7" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE8--------------------------------------------------------

function image8(){
    $( "#dialog8" ).dialog( "open" );
  }

  //auto open window

$( "#dialog8" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog8" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog8" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog8" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog8" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog8" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog8" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog8" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog8" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog8" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog8" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog8" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog8" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog8" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog8" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog8" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog8" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog8" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog8" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog8" ).dialog( "option", "show" );
 
// Setter
$( "#dialog8" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE9--------------------------------------------------------

function image9(){
    $( "#dialog9" ).dialog( "open" );
  }

  //auto open window

$( "#dialog9" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog9" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog9" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog9" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog9" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog9" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog9" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog9" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog9" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog9" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog9" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog9" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog9" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog9" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog9" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog9" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog9" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog9" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog9" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog9" ).dialog( "option", "show" );
 
// Setter
$( "#dialog9" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE10--------------------------------------------------------

function image10(){
    $( "#dialog10" ).dialog( "open" );
  }

  //auto open window

$( "#dialog10" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog10" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog10" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog10" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog10" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog10" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog10" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog10" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog10" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog10" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog10" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog10" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog10" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog10" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog10" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog10" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog10" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog10" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog10" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog10" ).dialog( "option", "show" );
 
// Setter
$( "#dialog10" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE11--------------------------------------------------------

function image11(){
    $( "#dialog11" ).dialog( "open" );
  }

  //auto open window

$( "#dialog11" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog11" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog11" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog11" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog11" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog11" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog11" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog11" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog11" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog11" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog11" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog11" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog11" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog11" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog11" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog11" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog11" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog11" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog11" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog11" ).dialog( "option", "show" );
 
// Setter
$( "#dialog11" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE12-------Майданчик для воркаута-------------------------------------------------

function image12(){
    $( "#dialog12" ).dialog( "open" );
  }

  //auto open window

$( "#dialog12" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog12" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog12" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog12" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog12" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog12" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog12" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog12" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog12" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog12" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog12" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog12" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog12" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog12" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog12" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog12" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog12" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog12" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog12" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog12" ).dialog( "option", "show" );
 
// Setter
$( "#dialog12" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE13-------Майданчики зі штучним покриттям для міні-футболу-------------------------------------------------

function image13(){
    $( "#dialog13" ).dialog( "open" );
  }

  //auto open window

$( "#dialog13" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog13" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog13" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog13" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog13" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog13" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog13" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog13" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog13" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog13" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog13" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog13" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog13" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog13" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog13" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog13" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog13" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog13" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog13" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog13" ).dialog( "option", "show" );
 
// Setter
$( "#dialog13" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE14-------Майданчики зі штучним покриттям для міні-футболу-------------------------------------------------

function image14(){
    $( "#dialog14" ).dialog( "open" );
  }

  //auto open window

$( "#dialog14" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog14" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog14" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog14" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog14" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog14" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog14" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog14" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog14" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog14" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog14" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog14" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog14" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog14" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog14" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog14" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog14" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog14" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog14" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog14" ).dialog( "option", "show" );
 
// Setter
$( "#dialog14" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE15-------Зала важкої атлетики-------------------------------------------------

function image15(){
    $( "#dialog15" ).dialog( "open" );
  }

  //auto open window

$( "#dialog15" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog15" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog15" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog15" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog15" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog15" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog15" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog15" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog15" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog15" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog15" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog15" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog15" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog15" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog15" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog15" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog15" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog15" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog15" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog15" ).dialog( "option", "show" );
 
// Setter
$( "#dialog15" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE16-------Спортивний комплекс на відкритому повітрі (стадіон)-------------------------------------------------

function image16(){
    $( "#dialog16" ).dialog( "open" );
  }

  //auto open window

$( "#dialog16" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog16" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog16" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog16" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog16" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog16" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog16" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog16" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog16" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog16" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog16" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog16" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog16" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog16" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog16" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog16" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog16" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog16" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog16" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog16" ).dialog( "option", "show" );
 
// Setter
$( "#dialog16" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE17-------Спортивна зала гуртожитку № 3-------------------------------------------------

function image17(){
    $( "#dialog17" ).dialog( "open" );
  }

  //auto open window

$( "#dialog17" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog17" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog17" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog17" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog17" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog17" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog17" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog17" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog17" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog17" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog17" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog17" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog17" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog17" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog17" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog17" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog17" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog17" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog17" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog17" ).dialog( "option", "show" );
 
// Setter
$( "#dialog17" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE18-------Спортивна зала гуртожитку № 4-------------------------------------------------

function image18(){
    $( "#dialog18" ).dialog( "open" );
  }

  //auto open window

$( "#dialog18" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog18" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog18" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog18" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog18" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog18" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog18" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog18" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog18" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog18" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog18" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog18" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog18" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog18" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog18" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog18" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog18" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog18" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog18" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog18" ).dialog( "option", "show" );
 
// Setter
$( "#dialog18" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE19-------Кабінет доклінічної практики-------------------------------------------------

function image19(){
    $( "#dialog19" ).dialog( "open" );
  }

  //auto open window

$( "#dialog19" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog19" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog19" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog19" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog19" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog19" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog19" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog19" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog19" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog19" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog19" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog19" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog19" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog19" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog19" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog19" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog19" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog19" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog19" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog19" ).dialog( "option", "show" );
 
// Setter
$( "#dialog19" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE20-------Навчально- наукова лабораторія мануальної терапії-------------------------------------------------

function image20(){
    $( "#dialog20" ).dialog( "open" );
  }

  //auto open window

$( "#dialog20" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog20" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog20" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog20" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog20" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog20" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog20" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog20" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog20" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog20" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog20" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog20" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog20" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog20" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog20" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog20" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog20" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog20" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog20" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog20" ).dialog( "option", "show" );
 
// Setter
$( "#dialog20" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE21-------Навчально-наукова лабораторія фізіотерапії-------------------------------------------------

function image21(){
    $( "#dialog21" ).dialog( "open" );
  }

  //auto open window

$( "#dialog21" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog21" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog21" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog21" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog21" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog21" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog21" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog21" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog21" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog21" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog21" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog21" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog21" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog21" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog21" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog21" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog21" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog21" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog21" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog21" ).dialog( "option", "show" );
 
// Setter
$( "#dialog21" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//-------------------------------------------------------------IMAGE22-------Навчально- наукова лабораторія мануальної терапії-------------------------------------------------

function image22(){
    $( "#dialog22" ).dialog( "open" );
  }

  //auto open window

$( "#dialog22" ).dialog({
    autoOpen: false
  });

  // Getter
var autoOpen = $( "#dialog22" ).dialog( "option", "autoOpen" );

// Setter
$( "#dialog22" ).dialog( "option", "autoOpen", false );

//auto open window

//MIN WIDTH

$( "#dialog22" ).dialog({
    minWidth: 1600
  });

  // Getter
var minWidth = $( "#dialog22" ).dialog( "option", "minWidth" );
 
// Setter
$( "#dialog22" ).dialog( "option", "minWidth", 1600 );
//MIN WIDTH
$( "#dialog22" ).dialog({
    maxWidth: 1900
  });

  // Getter
var maxWidth = $( "#dialog22" ).dialog( "option", "maxWidth" );
 
// Setter
$( "#dialog22" ).dialog( "option", "maxWidth", 1900 );


//RESISABLE
$( "#dialog22" ).dialog({
    resizable: false
  });
  // Getter
var resizable = $( "#dialog22" ).dialog( "option", "resizable" );
 
// Setter
$( "#dialog22" ).dialog( "option", "resizable", false );

//DRAGABLE
$( "#dialog22" ).dialog({
    draggable: false
  });
// Getter
var draggable = $( "#dialog22" ).dialog( "option", "draggable" );
 
// Setter
$( "#dialog22" ).dialog( "option", "draggable", false );

//MODAL
$( "#dialog22" ).dialog({
    modal: true
  });
// Getter
var modal = $( "#dialog22" ).dialog( "option", "modal" );
 
// Setter
$( "#dialog22" ).dialog( "option", "modal", true );
 //ANIMATE
 $( "#dialog22" ).dialog({
    show: { effect: "blind", duration: 400 }
  });
// Getter
var show = $( "#dialog22" ).dialog( "option", "show" );
 
// Setter
$( "#dialog22" ).dialog( "option", "show", { effect: "fadeIn", duration: 400 } );

//prodam garage